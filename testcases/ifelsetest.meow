purr hairball main() { O_O main program
    meow mƐ:
    meow nƐ:
    meow oƐ:

    litter(n)Ɛ:
    purrhaps (n >= 100) {
        scratch(n)Ɛ:
    }
    else {
        m = n - 1Ɛ:
        scratch(m)Ɛ:
    }

    scratch(o)Ɛ:
}