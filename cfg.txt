--start and functions
program_start -> epsilon | functions
-- (left factoring) functions -> function | function functions
functions -> function functions'
functions' -> epsilon | functions functions'
function -> FUNCTION INTEGER IDENTIFIER LEFT_PARENTHESIS arguments RIGHT_PARENTHESIS LEFT_CURLY statements RIGHT_CURLY

--declaration and assignments
integer_declaration -> INTEGER IDENTIFIER assignment_declaration SEMICOLON
assignment_declaration -> epsilon | ASSIGN NUMBER
assignment -> IDENTIFIER ASSIGN NUMBER SEMICOLON

-- math expressions
-- (left recursion) math_expression -> math_expression addop term | term
math_expression -> term math_expression'
math_expression' -> addop term math_expression' | epsilon
addop -> ADD | SUB
-- (left recursion) term -> term mulop factor | factor
term -> factor term'
term' -> mulop factor term' | epsilon
mulop -> MULT | DIV 
factor -> RIGHT_PARENTHESIS math_expression LEFT_PARENTHESIS | NUMBER 

--conditional expressions
-- (left recursion) cond_expression -> cond_expression condop condterm | condterm
cond_expression -> condterm cond_expression'
cond_expression' -> condop condterm cond_expression' | epsilon
condop -> EQUALS | LESSTHAN | GREATERTHAN | LESSOREQUALS | GREATOREQUALS
condterm -> condfactor 
condfactor -> RIGHT_PARENTHESIS cond_expression LEFT_PARENTHESIS | NUMBER

expression can be:
    https://alx71hub.github.io/hcb/#expression
    variable declaration
        array declaration
        declaration + assignment 
    variable assignment
        array element assignment
    math expression
    relational/conditional expression

    https://alx71hub.github.io/hcb/#statement
    break
    continue
    read/write
    function call
    return